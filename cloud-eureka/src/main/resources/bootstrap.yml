spring:
  application:
    name: cloud-eureka
  cloud:
    config:
      uri: http://127.0.0.1:8888
      fail-fast: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
